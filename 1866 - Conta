#include<stdio.h>
int main(){

    int N;

    scanf("%d", &N);

    for(int i = 0; i < N; i++)
    {
        int X;
        scanf("%d", &X);

        if(X % 2 == 0)
            printf("0\n");
        else
            printf("1\n");
    }
    return 0;
}
